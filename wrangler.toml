# Cloudflare Pages configuration for Farm Bonusrechner
# Deploy: npx wrangler pages deploy . --project-name=farm-bonusrechner

name = "farm-bonusrechner"
pages_build_output_dir = "."
compatibility_date = "2024-09-25"
